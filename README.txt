Instructions to run:
1.Need Node JS and NPM installed
2.Open Terminal in the 'blog' folder
3.type "npm install" to install the required modules(ejs, express, body-parser and realm)
4.type "npm run start" to run the Node app and navigate 127.0.0.1:5000(or localhost:5000) to view the website